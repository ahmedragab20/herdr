# Logs
*.log
*.log.*

# macOS
.DS_Store

# Runtime state (changes frequently, user-specific)
session.json
session-history.json
sessions/
herdr.sock
herdr-client.sock

# Plugin registry (regenerated by `herdr plugin ...`; contains hardcoded
# absolute paths to the local machine, so it is not portable across hosts)
plugins.json

# Plugin build artifacts
plugins/github/**/target/
plugins/github/**/Cargo.lock
plugins/github/**/.cargo/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

release-notes.json

plugins
